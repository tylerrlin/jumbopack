import React from "react";

const Sidebar = () => {
    return (
        <div className="md:flex hidden h-screen left-0 w-[200px] bg-sidebar">
            Sidebar
        </div>
    );
};

export default Sidebar;